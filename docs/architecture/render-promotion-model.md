# Render promotion model

The intended environment mapping is:

- `main` deploys to Production.
- Feature branches deploy to Staging or branch previews.

This document records intent only. The deployment issue will define exact Render services, filters, health checks, environment variables and rollback procedures.
