# Pagination contract

List endpoints use one-based pages, bounded page sizes and explicit sort direction. The shared parser coerces query-string numbers and rejects page sizes above the defined operational limit.
