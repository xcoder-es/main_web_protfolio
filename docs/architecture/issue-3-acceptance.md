# Issue 3 acceptance map

- Shared form, error, identifier, pagination and payment contracts: `packages/contracts`.
- Validated public and private runtime configuration: `packages/config`.
- Deterministic clock, ID and logger doubles: `packages/testing`.
- Browser import and credential-name protection: `scripts/check-browser-boundaries.mjs`.
- Contract, configuration and test-double coverage: package test directories.
